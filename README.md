# packet-tracer
DHCP relay
I created this lab to show how to use dhcp relay and how to create a dhcp server
For this project I used one router, two switches ,two access points, nine endpoint devices. 
One of the endpoint devices will be server to act as a dhcp server.
I am using two switches so we can have two different lan to work with
